# ── 品牌标识 (Brand Identity) ──────────────────────────────────────────────
# 获取/设置位置: 你自己决定。这是整个前端页面展示的中文或英文品牌名。
# 作用: 只要在这里修改一次，整个网站所有页面的标题、导航栏文字都会自动跟着变。
# 前台展示的品牌大名
VITE_APP_NAME=Your App Name

# 你的主域名 (不用加 https://，例如 moodspace.xyz)
VITE_BASE_DOMAIN=moodspace.xyz

# ── 后端 API 地址 (Backend API URL) ────────────────────────────────────────
# 获取/设置位置: 根据你的实际运行环境填写，**切勿以斜杠 (/) 结尾**。
# 作用: 前端向后端发送所有数据请求的基准地址。
# 
# 【开发者本地运行】时填写:
# VITE_API_BASE_URL=http://localhost:3000
#
# 【部署到 VPS 生产环境】时填写你绑定后端的真实公网域名:
# 后端公网 API 地址
VITE_API_BASE_URL=https://api.your-domain.com

# ── Supabase (PostgreSQL 数据库及防白嫖验证) ────────────────────────────────
# 获取位置: Supabase 控制台 -> 进入你的项目 -> 左下角 Project Settings -> API
# 
# 1. URL: 在 "Project URL" 下方
# Supabase 项目 API 地址
VITE_SUPABASE_URL=https://your-project.supabase.co
#
# 2. Key: 在同一页面的 "Project API keys" 下方
# ⚠️ 极其重要: 前端**必须且只能**使用带有 "anon" 和 "public" 标签的 Key！
# 绝不能把 service_role key 填在这里，否则会导致整个数据库被任何人随意修改！
# Supabase 前端专用的公有 Key
VITE_SUPABASE_ANON_KEY=your-anon-key-here
